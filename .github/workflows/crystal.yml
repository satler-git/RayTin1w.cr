name: Crystal CI

on:
  push:
    paths:
      - "src/**"
    branches: ["main"]
  pull_request:
    branches: ["main"]

jobs:
  Lint:
    runs-on: ubuntu-latest

    container:
      image: crystallang/crystal

    steps:
      - uses: actions/checkout@v3
      - name: Install dependencies
        run: shards install
      - name: Ameba Linter
        run: ./bin/ameba

  build:
    runs-on: ubuntu-latest

    container:
      image: crystallang/crystal

    steps:
      - uses: actions/checkout@v3
      - name: Build
        run: crystal build ./src/raytin1w.cr --release --no-debug
      - name: Create image
        run: ./raytin1w > traced.ppm
      - name: Commit file
        run: |
          git config --global user.email "119724672+satler-git@users.noreply.github.com"
          git config --global user.name "satler-git"
          git checkout -b trace-image
          git add ./traced.ppm
          git commit -m "Add traced.ppm"
          git push origin trace-image
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}